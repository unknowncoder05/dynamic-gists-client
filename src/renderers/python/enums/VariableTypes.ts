export enum VariableTypes{
    str = "str",
    int = "int",
    float = "float",
    number = "float",
    list = "list",
    array = "list",
}

export type VariableType = (
    string//"str" | "int" | "float" | "float" | "list"
)
